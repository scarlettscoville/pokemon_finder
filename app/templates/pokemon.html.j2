{% extends 'base.html.j2' %}
{% block title %}
Catch Pokémon
{% endblock %}

{% block content %}
    <h1>Choose your PokéTeam!</h1>
    <h4>Search for the Pokémon you would like to add to your team.</h4>
    <form action="{{url_for('main.pokemon')}}" method="POST">
        <div class="textbox_button">
        {{form.hidden_tag()}}

            <div class="mb-3">
                {{form.name.label(class="form-label")}}
                {{form.name(class="form-control w-25")}}

                {% for error in form.name.errors %}
                    <small style="color:red"></small>{{error}}</div>

                {% endfor %}

            </div>

            <div>
            {{form.submit(class="btn btn-danger form-control w-25 mb-3")}}
            </div>
        </div>
    </form>

{% if pokemons %}

{% for poke in pokemons %}
    <div class="card" style="width: 20rem; display: inline-block; width: 20%">
        <div class="for_image">
            <img src="{{poke.sprite}}" class="card-img-top" alt="...">
        </div>
        <div class="card-body">
            <h4 class="card-title text-center">{{poke.name}}</h4>
        </div>
        <table class="table table-borderless" style="text-align: left; width: 75%; margin:0px auto;">
            <tbody>
            <tr>
                <td scope="col">Experience:</td>
                <td>{{poke.experience}}</td>
            </tr>
            <tr>
                <td scope="col">Ability:</td>
                <td>{{poke.ability}}</td>
            </tr>
            <tr>
                <td scope="col">Attack:</td>
                <td>{{poke.attack}}</td>
            </tr>
            <tr>
                <td scope="col">HP:</td>
                <td>{{poke.hp}}</td>
            </tr>
            <tr>
                <td scope="col">Defense:</td>
                <td>{{poke.defense}}</td>
            </tr>
            </tbody>
        </table>
    </div>
{% endfor %}
{% endif %}
</div>
{% endblock %}